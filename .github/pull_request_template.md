## Summary

- 

## Verification

- [ ] `npm run lint`
- [ ] `npm run release:audit`
- [ ] `npm run release:audit:strict` after final public repository URLs are set
- [ ] `npm run package`
- [ ] Local Companion import tested with `crafty-bridge-*.tgz`

## Device Testing

- Companion version:
- Bridge version:
- Windows version:
- Device model:
- Device firmware:
- BLE adapter:

## Notes

- This Companion module communicates with the external Crafty Bridge HTTP service.
- The module must not require child process, filesystem, native addon, worker thread, or insecure algorithm permissions.
